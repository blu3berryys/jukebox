#pragma once

#include <cstddef>
#include <string>

namespace jukebox {

/**
 * Gracefully stolen from
 * https://stackoverflow.com/q/440133
 */
std::string random_string(size_t length);

}  // namespace jukebox
